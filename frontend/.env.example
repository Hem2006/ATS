# URL of the deployed backend API (no trailing slash).
# Local dev: leave unset, defaults to http://localhost:8001
# Production (Vercel): set to your Render backend URL, e.g. https://stitch-ats-backend.onrender.com
VITE_API_URL=
